**Source**: <br>
https://www.bilibili.com/video/BV1uronYREWR/?spm_id_from=333.337.search-card.all.click&vd_source=5c63af6701b1ba50b8d6ba3394d52dc8 <br>
https://github.com/MarkTechStation/VideoCode/blob/main/MCP%E7%BB%88%E6%9E%81%E6%8C%87%E5%8D%97-%E8%BF%9B%E9%98%B6%E7%AF%87/weather/pyproject.toml

# MCP Host
支持 MCP Protocol的软件

<img src="./pics/截屏2026-05-21 下午12.24.11.png" alt="Alt Text" width="500"> <br>

Cline是vs code的一个插件

# MCP Server
server这个词有点误导性，其实一个mcp server ≈ app <br>
它的背后只是一个程序（which 符合 mcp 规范）

<img src="./pics/截屏2026-05-21 下午3.04.36.png" alt="Alt Text" width="800"> <br>
- uvx 安装 python 程序
- npx 安装 Node 程序

最好先把command在terminal里面跑一下安装一下，不然第一次直接粘贴到mcpServer那边容易下载超时：
```bash
uvx mcp-server-fetch
```
<br>

MCP Server 格式：
```python
from typing import Any
import httpx
from mcp.server.fastmcp import FastMCP

mcp = FastMCP("weather", log_level="ERROR")

@mcp.tool()
async def get_forecast(latitude: float, longitude: float) -> str:
    """Get weather forecast for a location.

    Args:
        latitude: Latitude of the location
        longitude: Longitude of the location
    """
    # ... codes ...
    return "\n---\n".join(forecasts)


if __name__ == "__main__":
    mcp.run(transport='stdio')
```

创建一个MCP Server，只需要
- 一个python script [weather.py](./weather.py)
- [pyproject.toml](./pyproject.toml)

<br>
weather.py:<br>
<img src="./pics/截屏2026-05-21 下午3.09.52.png" alt="Alt Text" width="800"> <br>

<img src="./pics/截屏2026-05-21 下午2.51.09.png" alt="Alt Text" width="800"> <br>
* MCP协议并没有规定如何与模型进行交互（也就是cline ⇔ 模型 这边）
    - 比如 cline 通过 xml 和模型交互
    - cherry studio 通过 function calling 和模型交互

## 使用别人的MCP Server
- mcp.so
- mcpmarket.com
- smithery.ai 
有很多现成的mcp server
<br>
<br>


## 绕过mcp host和mcp server交互
只要格式符合 JSON-RPC 2.0，不需要通过mcp host就可以和mcp server通信

e.g. 直接使用terminal

<img src="./pics/截屏2026-05-21 下午10.27.48.png" alt="Alt Text" width="900"> <br>